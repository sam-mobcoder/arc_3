# 1. Repo Used in this Project

### - `PuLID`
```git clone https://github.com/ToTheBeginning/PuLID.git```

### - `IP-Adapter` 
```git clone https://huggingface.co/h94/IP-Adapter```


# 2. Download Models from Hugging Face

Run `download_models.py` and it will download and store the files in the `models` folder 


# 3. Commands
1.
```apt update apt install git-lfs -y git lfs install```

2.
```ls /root/arc_3/models/image_encoder/IP-Adapter/models/image_encoder```

3. You Must see
```config.json model.safetensors preprocessor_config.json```

4.
```cd /root/arc_3/models```
